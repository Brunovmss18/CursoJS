const nomes = ['bruno', 'vinicios', 'morais', 'santos'];

//unshift
nomes.splice(2, 0, 'thiago', 'camilo');
console.log(nomes);

//nomes.splice(índice atual, delete, elen1, elen2, elen3);
//pop
//const removidos = nomes.splice(-1, 1);

//shift
//const removidos = nomes.splice(0, 1);
